import 'package:flutter/material.dart';

class StoriesChat extends StatelessWidget {
  const StoriesChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Stories"),
    );
  }
}
